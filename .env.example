# ============================================================================
# BionicPRO Environment Variables
# ============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# IMPORTANT: Never commit .env to version control!
# ============================================================================

# ============================================================================
# REQUIRED VARIABLES (must be set for the system to work)
# ============================================================================

# Airflow Fernet Key - used to encrypt sensitive data (connections, variables)
# Generate with: python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# Example: Wv1Qp3R5t7Y9uBdEfGhJkLmNoP2sUwXz4a6C8i0q=
AIRFLOW_FERNET_KEY=

# BFF Encryption Key - used to encrypt tokens stored in Redis
# Generate with: python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# Must be a valid Fernet key (44 characters, base64-encoded)
ENCRYPTION_KEY=

# JWT Secret Key - used for signing/verifying JWT tokens in Reports Service
# Generate with: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
# Or: openssl rand -base64 32
JWT_SECRET_KEY=

# ============================================================================
# OPTIONAL VARIABLES (for additional features)
# ============================================================================

# Yandex ID OAuth credentials (for external identity provider)
# Register your application at https://oauth.yandex.ru/
# Set Callback URL: http://localhost:8080/realms/reports-realm/broker/yandex/endpoint
# Leave empty to disable Yandex ID integration
YANDEX_CLIENT_ID=
YANDEX_CLIENT_SECRET=
